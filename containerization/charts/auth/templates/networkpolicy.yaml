kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: open-pod-auth
spec:
  podSelector: 
    matchLabels: {}
  policyTypes:
  - Egress
  - Ingress
  egress:
  - to:
    - podSelector: 
        matchLabels:
          role: auth
    - ipBlock:
        cidr: 10.12.0.0/20
  ingress:
  - from:
    - podSelector: 
        matchLabels:
          role: auth
    - ipBlock:
        cidr: 10.12.0.0/20