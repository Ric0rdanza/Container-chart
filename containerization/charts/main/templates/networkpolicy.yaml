kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: open-pod-main
spec:
  podSelector: 
    matchLabels: {}
  policyTypes:
  - Egress
  - Ingress
  egress:
  - to:
    - podSelector: 
        matchLabels:
          name: main-chart
    - ipBlock:
        cidr: 10.12.0.0/20
  ingress:
  - from:
    - podSelector: 
        matchLabels:
          name: main-chart
    - ipBlock:
        cidr: 10.12.0.0/20