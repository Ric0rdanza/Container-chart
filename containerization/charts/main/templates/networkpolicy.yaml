kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: open-pod-main
spec:
  podSelector: 
    matchLabels: 
      app: main-chart
  policyTypes:
  - Egress
  - Ingress
  egress:
  - to:
    - podSelector: 
        matchLabels:
          app: main-chart
    - ipBlock:
        cidr: 10.12.0.0/20
  ingress:
  - from:
    - podSelector: 
        matchLabels:
          app: main-chart
    - ipBlock:
        cidr: 10.12.0.0/20